const add = (first , second) => {
    return first + second;
}



// const number =  [20, 24, 235, 89]
// const sumReducer = (previous, current) => previous + current;
// const total = number.reduce(sumReducer, 0)
export default add;